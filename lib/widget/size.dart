part of 'index.dart';

class EMHeight extends SizedBox {
  const EMHeight(double height, {super.key}) : super(height: height);
}

class EMWidth extends SizedBox {
  const EMWidth(double width, {super.key}) : super(width: width);
}
